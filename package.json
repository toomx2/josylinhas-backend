{
  "name": "josylinhas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "server": "npm run dev --prefix backend",
    "start": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "seeders": "npm run seed:admin --prefix backend"
  },
  "dependencies": {
    "concurrently": "^10.0.3"
  }
}